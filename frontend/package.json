{
  "name": "mecaconnect-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test --experimental-test-coverage tests/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
